Then /^the branch "([^\"]*)" should be created$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

Then /^the branch "([^\"]*)" should be merged into master$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end
